[build]
builder = "nixpacks"
buildCommand = "pnpm install && pnpm --filter @osikatsu-pro/worker build"

[deploy]
startCommand = "pnpm --filter @osikatsu-pro/worker start"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
